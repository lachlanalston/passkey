{
  "name": "passkey-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dev tooling only. The site itself is buildless — core.js, ceremonies.js and app.js ship to GitHub Pages exactly as authored.",
  "scripts": {
    "serve": "python3 -m http.server 8000",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
